# Backlog

Future tasks not yet scheduled into a sprint.

**Design references (added 2026-07-19):** before non-trivial UI/UX design decisions (new layouts, redesigns, pattern choices), check real examples on [mobbin.com](https://mobbin.com) (production app screens by flow), [refero.design](https://refero.design), and [recent.design](https://recent.design) rather than designing from scratch. Relevant to the still-open "bigger" items in this file's UI/UX audit section (desktop layout, shared dashboard chrome, stock-level signal).

**Playwright e2e suite added (2026-07-19, see decisions.md):** `npm run e2e` runs `e2e/*.spec.ts` against the local dev server (must be running on :8080 first). Most tests still log in via cookie injection (`e2e/auth-helper.ts`) for speed, but the real login UI (email+password since 2026-07-24, `e2e/login-signup.spec.ts`) and the sidebar auth state (`e2e/sidebar-auth-state.spec.ts`) are also covered live. **22/22 pass, 2 skip by design** (the payment-failure webhook tests need `RAZORPAY_WEBHOOK_SECRET` set — not part of normal dev setup, since this app has no real Razorpay account yet), including `full-order-lifecycle.spec.ts` — a complete customer→seller→partner order demo, all three roles in one test, each its own browser context. Key gotcha for any future seller-flow test: `/seller/orders` is gated by a **localStorage-only** verification state (`nearcart-verification-{shopId}`), not the DB `shop_verifications.overall_status` — must seed via `context.addInitScript()`, writing to the DB alone does nothing. A good next addition: a dedicated seller-side spec (Shop Hours editor, product management) reusing that same localStorage-seeding trick.

## Auth (2026-07-24, see decisions.md)

- [x] **Replaced email OTP with email + password** (no SMTP configured for this project, OTP delivery was unreliable) — see the fuller entry further down this file and decisions.md.
- [x] **Fixed a real sidebar/auth contradiction** — `SidebarDrawer.tsx`'s profile section was hardcoded fake data ("Sanket Kumar") with a permanent "Log Out" button that never called the real `logout()`, contradicting pages that correctly checked real auth state. Now reflects `useAuth()` for real; live-verified with `e2e/sidebar-auth-state.spec.ts`.
- [ ] **No password-reset flow** — deliberately deferred when OTP was dropped (would need email again, the exact dependency being removed). Worth a real decision later on whether/how to support it (e.g. a one-time SMTP provider just for reset emails, admin-assisted reset, etc.).

## OSM shop import + claim flow (2026-07-22/23, see decisions.md — marketplace cold-start fix)

- [x] **Built and live-verified.** `supabase/migrations/0011_osm_unclaimed_shops.sql` (applied) — `shops.owner_id` nullable, `source`/`external_id`/`claimed`/`claimed_at` columns, a check constraint, a partial unique index. `supabase/import-osm-shops.mjs` — real Overpass-sourced import. `claimShop`/`searchUnclaimedShops` (`seller-data/backend.server.ts`) + UI (`seller.tsx`'s choose screen, `ClaimShopStep.tsx`, empty states on `shop.$shopId.tsx`, an unclaimed-indicator pill on `ShopCard.tsx`). Tests: `e2e/claim-shop.spec.ts` (real claim through the UI + a two-merchant race test), extended `e2e/public.spec.ts` (empty-state coverage).
- [x] **Imported 9 Bengaluru areas** (Koramangala, Indiranagar, HSR Layout, BTM Layout, Whitefield, Jayanagar, Malleswaram, Marathahalli, Electronic City, MG Road/Central) — **5,732 active shops total**. Importer gained real multi-mirror Overpass fallback (`overpass-api.de` → `overpass.kumi.systems` → `overpass.private.coffee`) since the primary 504's under its own load fairly often — durable fix, not just for this run.
- [x] **Fixed the scale bug the expanded catalog exposed**: `getNearbyShops`/`checkServiceability` had no radius filter or pagination — PostgREST's 1000-row cap was silently hiding ~80% of the imported catalog from the home page, and `checkServiceability` could have been giving wrong answers for the same reason. Fixed with real indexed PostGIS queries (`supabase/migrations/0012_nearby_shops_postgis.sql`, applied — `nearby_shops`/`nearest_shop_distance_m` using the GiST index already on `shops.location`). Live-verified: correct radius filtering (a Mumbai coordinate correctly returns 0 Bengaluru shops), ~180ms warm vs. ~1.4s before. Full suite 17/17 pass.
- [ ] **Import more cities / areas beyond these 9 Bengaluru localities** — the importer is idempotent (upserts by `external_id`), safe to re-run over the same area or point it at a new one via `--lat/--lng/--radius-km`.
- [ ] **`way`/`relation` OSM geometries aren't imported** — node POIs only for this pass (deliberate scope cut, not a bug). Larger stores mapped as building outlines on OSM are invisible to the current importer; would need centroid computation to include them.
- [x] `searchShops` now applies the claimed+verified gate (2026-07-23) — still has no explicit row `.limit()` though, unlike `getNearbyShops`'s capped RPC. `searchProducts` is unaffected either way — lower risk since a text-filtered query naturally self-bounds in practice, but worth a look given the catalog's scale.
- [ ] **A real, unexplained root cause left open**: `claimShop`'s provision-verification-and-reread tail showed real, reproducible flakiness specific to the dev server's request handling (an update-then-immediate-read of the same row sometimes failed, including a real Postgres FK violation on a row that unquestionably existed) — the identical sequence run as a plain standalone Node script against the same project never reproduced it once. Currently mitigated with a short retry (`retryFlaky`, `seller-data/backend.server.ts`), not root-caused. Worth investigating if it resurfaces elsewhere (e.g. in other update-then-read backend sequences) — start by checking Supabase/Supavisor pooling mode and whether Vite dev SSR's dynamic `await import("./backend.server")` per-request pattern has any bearing.
- [ ] Geocoding a real fallback for missing OSM `addr:postcode`/`addr:city` (uses the same hardcoded Bengaluru defaults `createShop` already relies on today — consistent, not a regression, but not real either).

## Registered-shop gate, demo flag, duplicate detection, email+password auth (2026-07-23/24, see decisions.md)

- [x] **Real "claimed + verified" gate on customer browse** — `supabase/migrations/0013_verified_shop_gate.sql` (applied). `nearby_shops`/`nearest_shop_distance_m` (0012) now require `claimed=true` + `shop_verifications.overall_status='approved'`; `getNearbyShops`'s no-location fallback and `searchShops` gained the equivalent filter. `getShop`/`getShopProducts` deliberately stay ungated (direct links still work); `placeOrder` gained a real server-side check too, not just a browse-list cosmetic filter. Live-verified the gate genuinely toggles (flip one shop to `approved`, it appears; revert, it's gone again).
- [x] **`shops.is_demo` flag + junk cleanup** — same migration. `supabase/flag-demo-shops.mjs` flagged the 6 real seed shops, deleted 2 accidental junk rows ("showwww", "E2E Cancel Test Shop"). Bookkeeping only — demo shops are still subject to the same gate as everything else.
- [x] **Duplicate-shop suggestion at "create a new shop"** — `CreateShopStep.tsx` runs a debounced `searchUnclaimedShops` as the merchant types the name, offers "Claim one of these instead" (non-blocking). Not built: proximity-based matching (no real coordinate exists at signup time — `createShop` still hardcodes fixed lat/lng, a real pre-existing gap, not touched here) and an admin-side duplicate hint during verification review (the admin queue itself never receives anything today — see the `pending_review` gap below — so this would have no practical value until that's fixed).
- [x] **Admin verification queue received its first real submission (2026-07-26, see decisions.md)** — a real seller (`a26d2414-…`, "Test SHOP BTM") completed the wizard through L7 and reached `pending_review`; approved through the same `approveShop` path the UI uses. Surfaced a real, separate bug while approving it: see the `VerificationLockGate` fix below.
- [x] **Replaced email OTP with email + password** — this project has no custom SMTP configured, so OTP codes rode Supabase's default, heavily rate-limited email provider and could go undelivered (confirmed the API call itself works fine; delivery was the likely real issue). `auth-session/backend.server.ts`'s `signInWithPassword`/`signUpWithPassword` (the latter via the service-role Admin API with `email_confirm: true` — zero email dependency anywhere in auth now) replace `requestOtp`/`verifyOtp`. New `EmailPasswordAuth.tsx` (replaces deleted `EmailOtpAuth.tsx`) across all 10 call sites. Live-verified through the real UI (not cookie injection): signup, login, and wrong-password error all work. No password-reset flow yet (would need email again) — deliberately deferred.

## Order-lifecycle QA sweep findings (2026-07-22, see decisions.md — audit only unless marked fixed)

- [x] **Fix `advanceOrder`'s missing terminal-state guard** (`src/lib/seller-data/backend.server.ts` `setOrderStatus`/`advanceOrder`) — fixed 2026-07-22: `advanceOrder` now explicitly no-ops on `"new"`/`"rejected"` instead of the buggy `indexOf`-based arithmetic, and `setOrderStatus` performs a conditional `.eq("status", fromDbStatus)` update (optimistic concurrency) so `acceptOrder`/`rejectOrder`/`advanceOrder` can no longer land on a row that moved under them. Live-reverified: `e2e/order-race-resurrection.spec.ts` now passes as a real (non-`test.fail()`) regression test, and `full-order-lifecycle.spec.ts` still passes unaffected.
- [x] **Fix the two-partners-both-accept race** (`src/lib/partner-data/backend.server.ts` `acceptJob`, `src/lib/seller-data/backend.server.ts` `offerToPartner`) — fixed 2026-07-22: `acceptJob` now conditionally claims its own assignment row (`WHERE status='offered'`) and then conditionally claims the order itself (`WHERE status IN (preparing, ready_for_pickup)`); whichever partner's order-claim loses gets their assignment reverted to the (pre-existing, previously-unused) `'expired'` status instead of left dangling as `'accepted'`. `offerToPartner` also gained a first-line-of-defense check rejecting a second offer while one is already pending/accepted. Live-verified with a new test, `e2e/partner-double-accept-race.spec.ts`: two real partner sessions racing a real seeded double-offer via `Promise.all` — exactly one ends `accepted`, the other `expired`, exactly one `partner_assigned` order_event, order correctly lands on `partner_assigned`.
- [x] **Write `payment_failed` through to `orders.status`** (`src/lib/payments/backend.server.ts`) — fixed 2026-07-22: both `verifyPayment`'s signature-mismatch branch and `handleWebhookEvent`'s `payment.failed` branch now call a shared `markOrderPaymentFailed()` — conditional on the order still being `created` (guards a late/out-of-order webhook from clobbering an order a different path already marked `paid`), inserts a matching `order_events` row, and sends the customer a real "Payment failed" notification. Live-verified with `e2e/payment-failed-webhook.spec.ts` (2 tests, both require `RAZORPAY_WEBHOOK_SECRET` set — self-skip otherwise, see above) via a real signed HTTP POST to `/api/webhooks/razorpay`.
- [x] **Shop owners could place orders (and leave reviews) at their own shop** (`src/lib/orders/backend.server.ts`'s `placeOrder`, `src/lib/reviews/backend.server.ts`'s `getReviewableOrder`) — found and fixed 2026-07-22, not part of the original sweep. This app uses one unified login, so nothing stopped the same auth user who owns a shop from also ordering from it as a customer — the real risk being a self-delivered self-order getting a real review, inflating `rating_avg`. `placeOrder` now throws if `shop.owner_id === customerId`; `getReviewableOrder` independently rejects `shop.owner_id === callerId` as defense in depth. Live-verified with `e2e/self-order-guard.spec.ts`: a real checkout attempt at one's own shop is rejected with a real error toast and creates no order row; a self-order seeded directly (bypassing `placeOrder`) still can't be reviewed.
- [x] **`orders.idempotency_key` (real unique column) is never populated by `placeOrder`** — fixed 2026-07-22: `checkout.tsx` generates a key once per checkout attempt (`crypto.randomUUID()`, persisted in `sessionStorage` so it survives a page refresh, cleared only on confirmed success) and sends it as a required `idempotencyKey` field; `placeOrder` short-circuits to the existing order if one already exists under that key (checked up front, before any pricing/address/order-row work), and the insert itself is guarded by the column's real unique constraint — a genuine concurrent race with the same key gets a `23505` violation, handled by fetching and returning the winner instead of erroring. Live-verified with `e2e/placeorder-idempotency.spec.ts`: a real "Place Order" click's request is allowed to actually succeed server-side but its response is dropped (simulating a lost connection right after success); a real second click with the same persisted key returns the SAME order rather than creating a duplicate.
- [ ] **Real inventory: `products.stock_qty` is display-only** — `placeOrder`/`priceItems` never check or decrement it. Deliberate scope gap today, not a bug, but worth a real decision on whether to build actual stock decrement + oversell prevention.
- [ ] **Missing notifications:** admin force-cancel (`admin-data/backend.server.ts`'s `cancelOrder`) sends none; `offerToPartner`/`acceptJob` never notify the partner/customer/seller respectively — those roles only ever learn about it via client polling, not a real `notifications` row.
- [ ] **Seller dashboard UI: fire-and-forget mutations** — `seller.orders.tsx`'s `OrderCard` shows a success toast synchronously before its async server call resolves, has no `.catch` anywhere, and never disables a button while its request is in flight (Accept/Reject/Assign partner/Mark as X all affected).
- [ ] `placeOrder`'s `orders` + `order_items` inserts aren't transactional — an `order_items` failure after the `orders` insert leaves an orphan order.

---

## New feature: Reviews & Ratings (2026-07-19, see decisions.md)

- [x] **Built and mostly live.** `src/lib/reviews/{backend.server.ts,api.functions.ts}` — `submitReview`/`getReviewableOrder`/`getMyReviewForOrder` (authMiddleware, service-role for the assignments join RLS can't express to a customer) + public `listShopReviews`. `ReviewForm` (star inputs + comment) on `order.$orderId.tsx` once an order is delivered; `ShopReviews` list on `shop.$shopId.tsx`.
- [x] **`0009_review_rating_triggers.sql` confirmed applied (2026-07-19).** Live-checked by inserting a real 5-star review for a real shop with `rating_avg`/`rating_count` starting at 0 — both correctly recomputed to 5/1 immediately, confirming the trigger is active in the real database.

## Real shop/product photos (2026-07-19, see decisions.md — fully built and verified, no pending action)

- [x] New public Storage bucket `public-media` (created directly via the service-role key's Storage API, not SQL — verified genuinely public with a real anonymous fetch). `shops.logo_path`/`products.image_path` (existed since Phase A, previously unused) now wired end-to-end: `uploadShopLogo`/`uploadProductImage` in `seller-data/{backend.server.ts,api.functions.ts}` (real ownership check, 5MB/JPEG-PNG-WebP limit), `catalog/backend.server.ts` maps both into public URLs for every existing read path, `ShopCard`/shop header/`ProductCard` show the real photo with an emoji fallback, and `seller.settings.tsx`/`seller.products.tsx` have real upload controls. Live-verified end-to-end (upload, ownership check, public fetch, catalog mapping) — 9/9 checks passed.

## Feature candidates not built this pass (2026-07-19 research, ranked — see decisions.md)

- [x] **"Buy it again" / reorder (2026-07-19, see decisions.md).** `order.$orderId.tsx`'s "Order again" button now really re-adds the order's items — re-fetches the shop's current catalog (real prices/stock, not the historical snapshot), skips anything no longer available, tells the user how many were left out. Added `productId` to `OrderLine` (`order_items.product_id` was never selected before). **Caught a real stale-closure bug in `cart.tsx`'s `add()` before shipping** — calling it in a loop while switching shops would silently drop earlier items; fixed with a new `addMany()` batch method instead of a workaround. Verified live — 3/3 checks passed.
- [ ] **Referral program** — needs one small new `referrals` table, but the reward can reuse the existing `coupons` engine rather than a separate discount mechanism.
- [ ] **Loyalty points** — `wallet_ledger` exists (select-only for `authenticated`, no writer) and is schema-ready to double as a points ledger; needs a real earn/redeem flow designed.
- [ ] **Subscription membership** (free delivery, Prime-style) — blocked on real recurring billing; Razorpay integration itself is still scaffolded, not live.
- [ ] **Live order-support chat** — blocked on real-time infrastructure this app doesn't have at all (same gap as the still-unexercised Supabase Realtime).
- [~] **Real-time order/tracking updates via Supabase Realtime (`postgres_changes`) — Phase 1 built 2026-07-21, see decisions.md.** The 2026-07-19 note above missed a real second blocker: session cookies are `HttpOnly` by design, so the browser never gets a JS-readable JWT — a *direct client-side* subscription was never actually viable (RLS grants `select` only to `authenticated`, so an anon-key channel would silently receive nothing). Built as a **server-side SSE relay** instead (`src/routes/api.live.order.$orderId.ts` + `openOrderLiveSubscriptions()` in `src/lib/tracking-data/backend.server.ts`) for the customer order-detail page only — service-role key server-side, same trust boundary as every other backend function, streaming only that caller's authorized events. `tsc`/`eslint`/`vite build` clean; live-verified against the real Supabase project (real test user, real session, real `200 text/event-stream` response). **2026-07-21 update: migration run, end-to-end delivery confirmed live** — the publication was empty on the first check (needed a second pass by the user), then confirmed populated; a real `curl`-based check against the actual route (a real session, a real committed `orders.status` write) received the live event correctly. (An earlier `fetch()`-based check falsely reported failure — that was Node's undici client buffering slow-trickling stream chunks, not a real bug; see decisions.md.) **Still blocked on**: (1) a real Cloudflare preview-deploy spike to confirm `@supabase/realtime-js` actually opens a WebSocket in the Workers runtime (unverified — everything above was checked against `vite dev`/Node, not a deployed Worker, and the vendored library has a stale-looking Cloudflare-rejection code path worth spiking first); (2) confirm which Cloudflare plan is deployed (CPU-time headroom). A 45s safety-net poll stays active regardless of `live` state specifically so neither of those being unresolved can silently stall the page. Seller (`shop-orders`)/partner (`my-jobs`) live feeds are the same pattern, not yet built. Next natural step: extend `full-order-lifecycle.spec.ts` with a real live-update assertion now that there's a working publication to test against.
- [x] **`openapi.ts` documentation gap (2026-07-19, see decisions.md).** Added the 9 missing admin-data operations + 5 schemas, and full coverage for every 2026-07-18+ feature module (addresses/profile/wishlist/offers/notifications/reviews/shop-hours — 21 new operations, 10 new schemas). Verified live: fetched the real `/api-docs/openapi.json` and parsed it — 76 paths (up from 46), 39 schemas (up from 24), zero dangling `$ref`s.
- [x] **Shop Hours (2026-07-19, see decisions.md).** `shop_hours` table (day-of-week, open/close time, existed since Phase A, fully-correct RLS never exercised) now wired end-to-end — first module this session with zero service-role code, entirely RLS-enforced via `context.scopedClient`. `catalog/backend.server.ts`'s new `computeOpenStatus` derives real "open now"/next-opens label from hours, falling back to the manual toggle when a shop has none configured. `ShopCard`/shop detail page show the real label; new `ShopHoursEditor.tsx` lets sellers set their real weekly schedule from `/seller/settings`. Live-verified with two real seller accounts using real signed-in JWTs (not service-role) — 5/5 RLS ownership checks + 4/4 time-math scenarios passed.

## Post-Verification Features

- [ ] Level 8: Customer Verification — track delivery success, ratings, complaints, refund rate after launch
- [ ] AI Fraud Detection — image similarity check, reverse image search, shop board OCR vs entered name
- [ ] Periodic License Expiry Checks — auto-flag shops with expiring FSSAI, Drug License, etc.
- [ ] Multi-shop per owner — allow one owner to manage multiple shops with separate verifications
- [ ] Verification Analytics — admin dashboard showing approval rates, avg time to verify, rejection reasons

## Auth System (Prerequisite for Real Verification AND for all 3 account-creation flows — see plan/tasks/decisions.md 2026-07-10 and 2026-07-12)

- [x] Location/serviceability gate for the customer app — `src/lib/location.tsx` + `src/components/LocationModal.tsx`, wired into `AppShell` and `checkout.tsx`. GPS soft-ask → native prompt → fallback to manual pincode/area search → waitlist screen if no shop's 5km radius covers the location. **2026-07-22: now checks the real Supabase shops table** (`catalog/checkServiceability`, see decisions.md) instead of the mock `data.ts` list — see the item below, no longer deferred.
- [x] ~~Phone+OTP auth (dev-mode)~~ — superseded 2026-07-18, see below.
- [x] Auth-at-checkout for customers — `checkout.tsx` gates the checkout form behind login if not logged in (now `EmailOtpAuth`, was `PhoneOtpAuth`); cart is untouched since it's already a single localStorage store regardless of login state (no real per-account cart split exists yet, so there's nothing to "merge").
- [x] Route guards for seller/partner routes — `/seller/*` and `/partner/*` layouts now require login before rendering the dashboard.
- [x] ~~Route guard for `/admin/verification` — `isAdminPhone()`/`VITE_ADMIN_PHONES`~~ — superseded 2026-07-18: real `adminMiddleware` + `public.user_roles` check, see below. `src/lib/admin.ts` deleted.
- [x] Shop-creation step in front of the seller verification wizard — `src/components/seller/CreateShopStep.tsx` + `hasShop()`/`createShop()` in `src/lib/seller.tsx`. `SellerProvider` data (shop/products/orders) is now keyed per-account (`nearcart-seller-shop-${userId}` etc.) instead of one shared global key; a new account gets a genuinely empty shop (no demo products/orders) and must fill in name/business type/area before reaching the dashboard.
- [x] Partner registration step (vehicle type, basics) in front of the partner dashboard — `src/components/partner/CreatePartnerProfile.tsx` + `hasProfile()`/`createProfile()` in `src/lib/partner.tsx`. Same per-account keying fix; a new partner starts with zero jobs.
- [x] Verification storage keyed per-shop — `verificationStorageKey(shopId)` in `src/lib/verification.ts` replaces the old single global `VERIFICATION_STORAGE_KEY`; added `listAllVerifications()` to scan every `nearcart-verification-*` entry. `admin.verification.tsx` now merges every real shop's verification into its queue (alongside the existing mock demo entries) instead of only ever tracking one fixed shop, and `handleApprove`/`handleReject` write back through `loadVerification`/`saveVerification` scoped to the selected shop instead of a shared key.
- [x] `SUPABASE_SERVICE_ROLE_KEY` — was never actually invalid; `.env` had a missing newline between it and `SUPABASE_URL` on the same line, so the key's value was silently corrupted by `SUPABASE_URL="..."` getting appended to it. Fixed 2026-07-12 (own line now); re-tested directly against the live project and the admin client works. The verification pipeline's storage/audit writes in `backend.server.ts` should now actually work — worth a real end-to-end test of a document upload to confirm.
- [x] **Decided 2026-07-12:** real auth is Supabase Auth's native phone OTP, not a custom JWT/OTP service — matches the `auth.uid()`-based RLS policies already in `plan/03-database-schema.md` §5. See `plan/02-api-contracts.md` §0 and `decisions.md`. Superseded: hand-rolled JWT session management, custom refresh-token flow, custom RBAC middleware — Supabase Auth + `user_roles` + RLS covers all of it once migrated.
- [x] ~~Still needs an SMS provider~~ / ~~Replace `src/lib/auth.tsx`'s dev-mode localStorage session with real `supabase.auth` calls~~ — superseded 2026-07-18: pivoted to email OTP instead of waiting on a funded Twilio account (same wall mobile hit), see below. Phone auth stays a live backlog item for whenever Twilio gets funded — `public.users.phone` was deliberately made nullable (not removed) so both can coexist.

## Web app: real Supabase Auth session + authorization hardening (2026-07-18, see decisions.md — Phase 0/1/2 of the plan; Phases 3–6 below are the deferred roadmap)

- [x] **Phase 0/1 — Real email-OTP session.** `src/lib/auth.tsx` rewritten off `localStorage` entirely: `AuthUser` is now `{id, email}`, `AuthProvider` hydrates from a real server-verified `getCurrentUser()` call, and login goes through `src/lib/auth-session/{backend.server.ts,api.functions.ts,middleware.ts}` — real `supabase.auth.signInWithOtp`/`verifyOtp({type:'email'})`, session tokens set as `__Host-nc-at`/`__Host-nc-rt` `HttpOnly`/`Secure`/`SameSite=Lax` cookies (never touch client JS), `authMiddleware` re-verifies the access token against Supabase on every call (with silent refresh) and hands handlers a real `context.uid` + an RLS-scoped client. `PhoneOtpAuth.tsx` → `EmailOtpAuth.tsx`; `src/lib/auth-bridge/` deleted (no longer needed now that real sessions exist directly).
- [x] **Phase 2 — `admin-data/` locked down.** All 12 functions in `src/lib/admin-data/api.functions.ts` gated behind a new `adminMiddleware` (checks `public.user_roles` for `role='admin'`, layered on `authMiddleware`). Previously: zero checks at all — anyone with the URL could suspend any shop/partner or force-cancel any order. `isAdminPhone()`/`VITE_ADMIN_PHONES` retired; `src/lib/admin.ts` deleted; `admin.tsx`'s layout gate now calls a real `checkAdminAccess()` server function. Bootstrapped one real `user_roles` admin row for the operator (`sanketavaralli321@gmail.com`) since the table was empty. Verified live through the actual dev server's RPC endpoint (not just logic in isolation): no session → `Unauthorized`, real non-admin session → `Forbidden`, real admin session → real data.
- [x] **Phase 3 — `orders/`, `tracking-data/`, `payments/verifyPayment` ownership checks (2026-07-18, see decisions.md).** `authMiddleware` attached to `placeOrder`/`listOrders`/`getOrder`, `getOrderTracking`/`pushPartnerLocation`, and `verifyPayment`. `customerId`/`partnerId` removed from every client-facing validator — all derived from `context.uid` server-side instead. `getOrder`/`getOrderTracking` return the same "not found"/no-leak default for both a genuinely-missing order and a not-yours order (can't be used to enumerate valid ids). `pushPartnerLocation` looks up the real `delivery_partners.id` from the caller's own uid rather than trusting a client-supplied `partnerId`. Found and fixed a real gap in passing: `order.$orderId.tsx` had no auth gate at all — added the standard `authLoading`/`!user` pattern. Kept on the service-role client throughout — the `orders` GRANT wall (no `authenticated` insert/update) is unchanged, this phase is about who's allowed to ask, not which client executes the query. Verified live: two real test accounts, a real order — owner sees their own data, a second caller gets the no-leak default instead, 9/9 checks passed.
- [x] **Phase 4 — `seller-data/` ownership checks (2026-07-19, see decisions.md).** `assertShopOwner`/`assertProductOwner`/`assertOrderOwner` added to every function (`updateShop`, `getMyProducts`, `addProduct`, `updateProduct`, `removeProduct`, `toggleStock`, `getShopOrders`, `acceptOrder`, `rejectOrder`, `advanceOrder`, `offerToPartner`, `syncVerificationSummary`) — all derive `callerId` from `context.uid` via `authMiddleware` now, none accept a client-supplied owner id anymore. `updateShop`/`updateProduct`'s `z.record(z.string(), z.any())` validators replaced with explicit schemas (the backend already whitelisted the same fields internally, so this wasn't exploitable, but the untyped API contract was bad hygiene). `advanceOrder` no longer trusts a client-claimed `currentStatus` — derives the real status from the order's own DB row first. **Found and fixed a real self-approval bug in passing:** `syncVerificationSummary` let a seller set `overall_status` to literally any string, including `"approved"` — added a safelist so a seller can only ever set `"incomplete"`/`"pending_review"`, never the admin-only terminal states. Verified live with two real seller accounts: owner passes every ownership check, a second account is rejected on all of them — 9/9 checks passed.
- [x] **Phase 5 — `partner-data/` ownership checks (2026-07-19, see decisions.md).** `getOwnPartnerId(callerId)` resolves the caller's real `delivery_partners.id` from their session — `getMyProfile`/`createProfile`/`toggleOnline`/`getMyJobs` no longer accept a `partnerId` parameter at all, eliminating the spoofing surface entirely rather than just checking it. `assertAssignmentOwner` gates `acceptJob`/`declineJob`/`advanceJob`. **Kept on the service-role client with explicit ownership checks, not flipped to RLS as the original plan suggested** — simpler and consistent with Phase 4's seller-data pattern; the RLS-flip remains a valid future option (`assignments_partner_all` policy already exists) but wasn't done this pass. **Found and fixed a real bug in passing:** `advanceJob` trusted a client-supplied `currentStatus`, the same bug class as Phase 3/4 — a caller could claim `"picked_up"` on a job barely offered to instantly credit themselves the full delivery payout; now derives the real status from the assignment's own DB row. Verified live with two real partner accounts — 4/4 checks passed.
- [x] **Phase 6 — `verification/` ownership checks (2026-07-19, see decisions.md). Closes out the entire 6-phase authorization-hardening plan.** This module was entirely unauthenticated, keyed only by `merchantRef` — a random client-generated UUID with zero stored link to a real shop/owner. The most severe of all six phases: PAN/Aadhaar/GST/bank documents, the most sensitive PII in the app, were reachable by anyone who knew or guessed a merchantRef. `analyzeFile`/`getSubmission`/`finalizeSubmission`/`getSignedFileUrl` now all require `shopId` + `authMiddleware`, verify real ownership, and — critically — `shopId` is now stored in the `events` table alongside `merchantRef` at write time, with every read filtering by both. A leaked/guessed `merchantRef` alone now grants nothing without a matching, ownership-verified `shopId`. Verified live with two real seller accounts and real shops — 6/6 checks passed, including the key scenario: shop B gets zero rows even querying with shop A's exact real merchantRef.
- [x] **Phase 7 — cleanup/re-sweep (2026-07-19, see decisions.md). Closes the entire original 7-phase authorization-hardening plan.** Re-sweep found one real gap Phases 3–6 missed: `seller-data/getAvailablePartners` had zero auth middleware at all — any unauthenticated caller could scrape every active delivery partner's real name + phone number. Fixed (caller must own a real shop). Dead-code check found `auth-bridge`/`isAdminPhone` already fully removed in earlier phases; the one real find was `openapi.ts` still documenting the retired phone-bridge `ensureRealUser` RPC as if it existed, plus several other stale doc strings (falsely claiming no server-side authorization, admin auth still a phone allowlist, `quoteOrder` uncalled by any UI) — all fixed. Verified live with a real seller + a real non-seller account — 2/2 checks passed. Real Supabase Realtime (`postgres_changes`) is now unblocked (was blocked on "no real session in the browser," resolved in Phase 1) — a good future candidate, not built this pass.

## Sidebar destinations that had no page at all (2026-07-18, see decisions.md)

- [x] **`/addresses`, `/notifications`, `/settings`** — real CRUD/read/edit, built directly on `authMiddleware`'s request-scoped client (no service-role) against the already-existing `addresses`/`notifications`/`users` tables and their owner-scoped RLS policies from Phase A, which had never been exercised by any app code before this. Live-verified with a real two-user test: owner can read/write their own rows, a second user's identical calls are silent no-ops (RLS enforcing, not just app-level filtering). `src/lib/addresses/`, `src/lib/notifications/`, `src/lib/profile/`.
- [x] **`/wishlist`, `/offers`** — `supabase/migrations/0007_wishlist.sql`/`0008_coupons.sql` applied by the user 2026-07-18 (confirmed live: both tables exist). `src/lib/wishlist/` follows the same scoped-client pattern as addresses; `src/lib/offers/` is public-read (service-role backend, mirrors `catalog/`'s convention — no owner to scope by). Added a heart-toggle to `ProductCard.tsx` (`shop.$shopId.tsx`, `search.tsx`). Ran `supabase/seed-coupons.mjs` — 3 real demo coupons live (`WELCOME50`, `FREESHIP`, `SAVE100`).
- [x] **`/payments`** — payment history (method/amount/status per past order), reusing the existing `orders/api.functions.ts` `listOrders` rather than new backend. No saved-card storage exists or is needed — Razorpay Checkout handles cards/UPI/netbanking fresh each time (Phase F).
- [x] **`/help`, `/about`** — static real content (FAQ accordion, `mailto:`/`tel:` contact links, "how NearCart works" copy). **The support email/phone shown (`support@nearcart.app`, `1800-000-000`) are placeholders** — swap for real contact details when available.
- [x] **Coupon redemption at checkout (2026-07-19, see decisions.md).** `checkout.tsx` now has a real coupon input wired to a new `applyCoupon` in `orders/backend.server.ts` — client sends only a code (never a discount amount; the validator schema has no such field), server recomputes the real discount on both `quoteOrder` (preview) and `placeOrder` (the real charge). **Caught and fixed a real unit-mismatch bug before shipping**: `coupons.min_order_amount`/`discount_value` are stored in rupees (unlike every other paise-denominated `_amount` column in the schema) — the first version compared paise against rupees directly. Live-verified against the 3 real seeded coupons + edge cases — 9/9 checks passed.
- [x] **Notification producers (2026-07-19, see decisions.md).** New `notifications/backend.server.ts` (shared `insertNotification` helper, service-role — the table only grants `select`/`update` to `authenticated`). Wired into the three real state-transition choke points: `seller-data`'s `setOrderStatus` (accept/reject/advance), `partner-data`'s `advanceJob` (picked_up/delivered — previously bypassed `setOrderStatus` entirely, so partner-driven transitions had zero notification path before this), and `admin-data`'s shop/partner approve/reject/suspend/reactivate functions. Live-verified with 4 real accounts — 7/7 checks passed (correct titles/recipients, RLS still isolates each user's own rows).

## UI/UX audit findings (2026-07-18, forked research + codebase review — see decisions.md for what's applied)

Ranked by impact vs. effort:

- [x] **Quick win, applied 2026-07-18:** `SidebarDrawer.tsx`'s per-item favorite/pin button was `opacity-0 group-hover:opacity-100` only — added `group-focus-within:opacity-100` + a `focus-visible` ring, now keyboard-reachable.
- [x] **Quick win, applied 2026-07-18:** icon-only buttons (`AppHeader.tsx`'s menu/cart) bumped `h-9 w-9` → `h-11 w-11` (44px touch target). `SidebarDrawer.tsx`'s desktop-only collapse toggle left as-is — it's `hidden md:flex`, never rendered on a touch viewport.
- [x] **Checked 2026-07-18, no fix needed:** computed the real WCAG contrast ratio for `--muted-foreground` (OKLCH→sRGB→relative luminance, not an eyeball guess) — passes AA comfortably in both themes (5.28–7.07:1 against the 4.5:1 threshold). The audit's contrast concern didn't hold up. The 9–10px font sizes used in places are still a legibility judgment call independent of contrast — not changed, left for the user to decide.
- [x] **Quick win, applied 2026-07-18:** `index.tsx`'s `getNearbyShops` query gained a real error/retry state (was loading/success only).
- [x] **Applied 2026-07-18:** replaced spinner loading states with content-shaped skeletons on `index.tsx` (`ShopCardSkeleton`) and `orders.tsx` (`OrderCardSkeleton`), using the previously-unused `skeleton.tsx` primitive. Other spinner-only pages (admin, seller, partner dashboards) not converted this pass.
- [x] **Applied 2026-07-19, see decisions.md:** `AppShell.tsx` gained an opt-in `wide` prop (`max-w-6xl`) used by `index.tsx`/`shop.$shopId.tsx`/`search.tsx`, whose shop/product grids now go `md:grid-cols-2`/`lg:grid-cols-3`. Every other page stays at the original narrow width by default. Also fixed `BottomNav.tsx` rendering under the desktop sidebar with no `md:hidden` — found while investigating this.
- [x] **Applied 2026-07-19, see decisions.md:** `ProductCard.tsx` now shows "Only N left" (destructive color, replacing the unit line) when `stockQty` is set and ≤5. `stockQty` added to `CatalogProduct`/`Product` types and mapped from the already-fetched `stock_qty` column.
- [x] **Applied 2026-07-19, see decisions.md:** new shared `src/components/dashboard/StatCard.tsx`, adopted by `seller.index.tsx`/`partner.index.tsx` (previously byte-for-byte identical inline tile markup) and `admin.index.tsx` (previously its own third bespoke card). The three dashboard *shells* (`SellerShell`/`PartnerShell`/`AdminShell`) were deliberately left un-merged — admin is a real desktop back-office layout, seller/partner are genuinely mobile-first, matching how those roles actually use the app.

## Location gating rebuilt as a dismissible modal (2026-07-18, user's request, see decisions.md)

- [x] `src/components/LocationGate.tsx` (full-screen hard block on every page until GPS/manual area was set) deleted, replaced by `src/components/LocationModal.tsx` — a dismissible `Dialog` with the same prompt/manual-entry/waitlist content, plus a "Maybe later — just let me browse" option. `AppShell.tsx` no longer blocks rendering on location status; it always shows the shell, auto-opening the modal once on first visit (`status === 'unset'`) and reopening it anytime via the header's location pill. `getNearbyShops` already degraded gracefully with no coordinates (all active shops, unsorted) — no backend change was needed, just wiring the UI to actually allow that path.
## Backend Build-Out (Phased Plan — see plan/02-api-contracts.md §0 for the module structure and transport model)

Each phase replaces one domain's localStorage mock with real Supabase-backed server functions, without changing the React Context/hook shape those components already use (same pattern as the 2026-07-06 "frontend-first" decision anticipated) — so this can ship incrementally, one phase at a time, without a big-bang rewrite.

- [x] **Phase A — Foundation.** `supabase/migrations/0001_initial_schema.sql` (all tables/enums minus `otp_codes`, RLS + GRANTs, `has_role()`, `on_auth_user_created` trigger) applied 2026-07-12; all 25 tables confirmed live. `0002_fix_orders_assignments_rls_recursion.sql` applied same day, fixing a real infinite-recursion bug in the `orders`↔`assignments` RLS policies (see decisions.md). Public-table RLS (shops/products/categories/reviews/shop_hours) confirmed correct via direct anon-key query. **Not yet fully verified:** owner-only tables (`users`/`user_roles`/`wallet_ledger`/`kyc_documents`) are still empty, so "anon sees 0 rows" can't yet be distinguished from "RLS isn't actually restrictive" — needs a real signed-up user to pressure-test properly.
- [ ] Enable the phone auth provider + an SMS integration (Twilio/MSG91) in Supabase Auth settings — dashboard action, needed before `signInWithOtp({ phone })` can send real SMS and before the owner-only RLS policies above can be properly pressure-tested with a real user.
- [x] **Phase B — Catalog.** `src/lib/catalog/{backend.server.ts,api.functions.ts}` — `getCategories`, `getNearbyShops`, `getShop`, `getShopProducts`, `searchCatalog`. Seeded via `supabase/seed.mjs` (idempotent, run with `node supabase/seed.mjs`) using a placeholder seed-owner account created through the Auth Admin API. Wired into `index.tsx` (home, via TanStack Query), `shop.$shopId.tsx` (via a route `loader` — needed for real per-shop SEO title/description, which a client-only `useQuery` can't provide), and `search.tsx`. Required two schema extensions beyond the original schema doc: `0003_catalog_display_fields.sql` (shops.tagline/emoji/delivery_fee_amount/free_delivery_above_amount/eta_minutes; products.emoji/menu_section — fields the real UI needs that the textbook schema didn't have) and `0004_geo_autofill_trigger.sql` (auto-derive `location` from `lat`/`lng`, since nothing was populating that PostGIS column otherwise). Verified live: real shop/product data confirmed via direct DB query, SSR title/meta on the shop detail page, and all three route modules compiling without the import-protection error below.
- [x] **CSRF gap fixed (2026-07-17, see decisions.md).** `src/start.ts` now uses the framework's own `createCsrfMiddleware({ filter: (ctx) => ctx.handlerType === 'serverFn' })` (the exact snippet from TanStack Start's own startup warning — confirmed by reading its source rather than guessing), added to `requestMiddleware` alongside the existing `errorMiddleware`. Scoped to server functions only, so `api.webhooks.razorpay.ts` (a server route receiving real cross-origin POSTs from Razorpay, no browser Origin header to check) is correctly unaffected — it relies on its own HMAC signature check instead. Verified live: a header-less POST to `/_serverFn/*` now gets a 403 (previously would have gone through), the same request with `Origin`/`Sec-Fetch-Site: same-origin` headers passes the CSRF check and proceeds to the next real error (invalid function id), and the webhook route's own 400 (missing signature) is unchanged, not overridden by a 403.
- [x] **API docs added (2026-07-17, see decisions.md).** `/api-docs` — self-hosted Swagger UI (`swagger-ui-dist`, no CDN) rendering a hand-maintained OpenAPI 3.0 spec (`src/lib/api-docs/openapi.ts`, served as JSON via `src/routes/api-docs.openapi[.]json.ts`) covering all 43 server functions across every domain (catalog/orders/payments/seller/partner/admin/tracking/auth/verification) plus the one real REST route (the Razorpay webhook). These are TanStack Start RPCs dispatched via internal hashed paths, not real REST routes, so the spec uses readable `/rpc/<module>/<fn>` paths purely for grouping/navigation — documented explicitly in the page's banner and the spec's own `info.description`, since "Try it out" doesn't work against them. The spec is manually maintained (not generated at build time from the Zod validators) — update it when server functions change.
- [x] **Real GST/FSSAI/PAN registry verification, scaffolded (2026-07-17, see decisions.md).** `src/lib/doc-verify/backend.server.ts` — real Deepvue KYB API integration (`verifyGst`/`verifyFssai`/`verifyPan`, GSTIN/FSSAI/PAN format regexes, name-similarity scoring), gated behind `DEEPVUE_CLIENT_ID`/`DEEPVUE_CLIENT_SECRET` (not configured today, same "scaffolded not live" pattern as Phase F payments). Wired into `verification/backend.server.ts`'s `analyzeFile()`: for gst/fssai/pan documents, cross-checks the OCR-extracted number against the real government registry (GSTN/FoSCoS) — a materially stronger signal than the existing AI-vision-only pipeline, since it confirms the number actually exists and is active rather than just judging whether the image looks plausible. A confirmed-invalid registry hit forces REJECTED; a confirmed-active hit with a matching name can upgrade an UNDER_REVIEW decision to VERIFIED. Zero effect on today's pipeline (confirmed no `DEEPVUE_*` keys in `.env`). Aadhaar (regulated, needs UIDAI-licensed AUA/KUA handling), Udyam (async create+poll pattern), drug_license, trade_license, shop_establishment, pharmacist_reg, and vehicle_rc are NOT wired up — researched and confirmed no clean public API exists for the drug-license/pharmacist-registration/trade-license/shop-establishment group at all (government portals only, no third-party API coverage found).
- [x] **`location.tsx`'s serviceability check migrated to the real DB (2026-07-22, see decisions.md).** The "identical coordinates" assumption this item was deferred on no longer held (a real E2E test shop and presumably real seller-onboarded shops exist outside the mock list) — user hit it directly via "select current location" not behaving right. Now calls `catalog/checkServiceability` (real Supabase `shops` table, `status='active'`, same JS-haversine approach as `getNearbyShops`), async as anticipated — `setLocation` returns a `Promise<LocationStatus>`, `LocationModal` shows a spinner during the round-trip. `geocodeArea`/`knownAreas` (the manual area-search picker) are still mock — no real geocoding API integrated, unchanged scope for this pass.
- [x] **Phase C — Cart & orders.** `src/lib/orders/{backend.server.ts,api.functions.ts}` — `quoteOrder`, `placeOrder`, `listOrders`, `getOrder` (`cancelOrder`/`reviewOrder`/`trackOrder` not yet built — no UI currently needs them). `src/lib/orders.ts` (localStorage) deleted; `cart.tsx`, `checkout.tsx`, `orders.tsx`, `order.$orderId.tsx` all wired to the real backend and to `src/lib/catalog/api.functions.ts`'s real `getShop` (fixing the Phase-B-caused regression noted above). Verified end-to-end with a real order insert against the live DB — correct pricing (items + free-delivery threshold + flat handling), and the `getOrder`/`listOrders` join shape confirmed to match what the UI needs.
- [x] **Auth bridge (new, needed to unblock Phase C):** `src/lib/auth-bridge/{backend.server.ts,api.functions.ts}` — dev-mode phone+OTP login (`src/lib/auth.tsx`) now calls `ensureRealUser()` on successful verify, which creates-or-fetches a **real** Supabase Auth user via the Admin API (no SMS provider needed) so every logged-in account has a real `public.users` row and can satisfy `orders.customer_id`/etc. foreign keys. Found and fixed a real bug during verification: Supabase stores `auth.users.phone` *with* the country code and no `+` (e.g. `918618587196`), but the first version of this bridge searched using the raw 10-digit number the app collects — every login would have silently tried (and failed) to recreate the same user. Delete `auth-bridge/` once a real SMS provider is configured and `auth.tsx` calls `supabase.auth.signInWithOtp` directly.
- [x] **`reviewOrder` and live `trackOrder` are now real** (stale line — both were built later this session/prior sessions and just never checked off here): `submitReview`/`getReviewableOrder` (reviews module, 2026-07-19) and `tracking-data/getOrderTracking` + `LiveTrackView` (real partner GPS, not the old simulation) are both live.
- [x] **Customer-initiated `cancelOrder` (2026-07-19, see decisions.md).** `orders/backend.server.ts`'s new `cancelOrder` — stricter than admin's force-cancel (only allowed before the shop starts preparing), real ownership check, real `order_events` audit row, notifies the shop owner. New "Cancel order" button on `order.$orderId.tsx`. Live-verified with two real customer accounts and orders in different statuses — 6/6 checks passed.
- [ ] `seller.track.$orderId.tsx`/`admin.verification.tsx` still reference the mock `data.ts` `getShop` in ways unrelated to the customer order flow (seller's own synthetic shop id, and the verification queue's shop-name lookup) — separate, pre-existing situations, not part of Phase C.
- [x] **Phase D — Seller ops.** `src/lib/seller-data/{backend.server.ts,api.functions.ts}` — real shop CRUD, product CRUD, order accept/reject/advance (§8). `SellerProvider` in `src/lib/seller.tsx` rewritten to fetch/mutate through these instead of localStorage, while keeping the exact same `useSeller()` Context shape — none of `seller.index.tsx`/`seller.orders.tsx`/`seller.products.tsx`/etc. needed to change. `hasShop()`/`createShop()` are now async (Supabase queries), which also required converting `CreateShopStep.tsx` and the `/seller` route's shop-creation gate to `useQuery`-based loading states. Added `syncVerificationSummary()` so the verification wizard (still localStorage) at least pushes its badge/business-type/overall-status summary to the real `shop_verifications` row — otherwise a seller completing verification would never show as verified to real customers browsing Phase B's catalog. Extended `categories` with `restaurant`/`retail`/`salon`/`home_business` (business types sellers can pick that had no matching discovery category). Verified end-to-end against the live DB: shop creation, product add/toggle-stock, and order accept→advance status transitions with correct audit trail — all via direct script, not just typecheck.
- [ ] `partners`/`assignPartner` in `SellerContext` are still the seeded constant list / a no-op — real delivery-partner accounts are Phase E's job.
- [ ] The verification wizard's full progress (documents, KYC, bank, GPS) is still localStorage-only; only its summary fields sync to the real DB now. Full migration off the generic `events` table remains a separate, already-tracked item.
- [x] **Phase E — Partner ops.** `src/lib/partner-data/{backend.server.ts,api.functions.ts}` — real profile/registration, online toggle, job list, accept/decline, pickup/deliver. `src/lib/partner.tsx`'s `PartnerProvider` rewritten the same way `seller.tsx` was in Phase D — real fetch/mutate underneath, identical `usePartner()` shape on top. `hasProfile()`/`createProfile()` became async (same ripple as Phase D: `CreatePartnerProfile.tsx` and the `/partner` route gate both needed updating to loading-state patterns). Also wired the seller side of dispatch that Phase D left as a no-op: `getAvailablePartners()`/`offerToPartner()` in `seller-data/`, so a seller assigning a partner now creates a real `assignments` row instead of doing nothing. `placeOrder` (Phase C) updated to actually generate `pickup_otp`/`delivery_otp` — it never had before. Job flow simplified from the mock's 4 stages (accepted→at_shop→picked_up→delivered) to 3 (accepted→picked_up→delivered) — the real `order_status` enum has no "arrived at shop, not yet collected" state distinct from `partner_assigned`, so that stage was dropped rather than faked; `partner.deliveries.tsx`'s `at_shop` references updated to match. Payout is a flat distance-based formula (₹25 + ₹8/km) — no real dispatch-economics engine exists. Verified end-to-end against the live DB: registration → online toggle → offer → partner sees it as a new job → accept (order and assignment both update, visible from the seller's own query too) → pickup → deliver with earnings recorded.
- [x] **Phase F — Payments (scaffolded, not live — see decisions.md 2026-07-15).** Still no Razorpay account/keys, so built the full integration gated behind their presence rather than waiting further. `src/lib/payments/{backend.server.ts,api.functions.ts,checkout-widget.ts}`: `createRazorpayOrder()`, `verifyPayment()` (HMAC signature check, never trusts the client), `verifyWebhookSignature()`/`handleWebhookEvent()` (idempotent). Real HTTP webhook endpoint at `src/routes/api.webhooks.razorpay.ts` (`/api/webhooks/razorpay`), using TanStack Start's `server.handlers` file-route convention (first use of this pattern in the app — confirmed via the framework's own bundled skill doc, not guessed). `orders/backend.server.ts`'s `placeOrder` now checks `isConfigured()`: if unset (true today), behaves exactly as before (non-COD instantly "paid" — zero regression); if configured, the order starts unpaid (`status: 'created'`) and a real Razorpay order is created alongside it. `checkout.tsx` opens the Checkout.js widget only when `order.payment?.required` comes back true. The HMAC signature logic and the DB-side read/write flow of `verifyPayment` were independently verified (fake secret, since Razorpay's real API can't be exercised without an account) — the actual Razorpay REST API calls (`createRazorpayOrder`) and the Checkout.js widget itself are unverified against a live gateway. Add `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET`/`RAZORPAY_WEBHOOK_SECRET` to `.env` (server-only, no `VITE_` prefix needed) to go live — no further code changes required.
- [ ] Payment retry UI — if a customer dismisses the Razorpay widget, the order row already exists (unpaid, `status: 'created'`) but there's no way to resume payment from `/orders`; currently the only guidance is "place it again," leaving the abandoned order dangling. Not built this pass.
- [x] **Phase G — Admin (scoped: verification queue only, see decisions.md 2026-07-14).** `src/lib/admin-data/{backend.server.ts,api.functions.ts}` — `listShopsForReview()`, `approveShop()`, `rejectShop()`, reading/writing the real `shop_verifications.overall_status`. `admin.verification.tsx` now lists real shops with `overall_status = 'pending_review'` instead of `listAllVerifications()`'s localStorage scan + `MOCK_PENDING_SHOPS` (both removed). Deep verification detail (documents/KYC/bank/GPS) is still localStorage-only, so the detail panel falls back to a "not available on this device" message for shops whose wizard data isn't in this browser, instead of crashing — approve/reject still work from the summary alone. Verified end-to-end against the live DB: a shop with `pending_review` status appears in the queue, approving it updates the real row and it drops out of the queue.
- [x] **Full admin dashboard built (2026-07-17, see decisions.md).** `src/routes/admin.tsx` — new shared layout route (login + `isAdminPhone` gate + tab nav, matching `seller.tsx`'s pattern), with `admin.verification.tsx` simplified to drop its now-redundant inline gate. New pages: `admin.index.tsx` (`/admin` — live aggregate stats: shop/partner/order counts by status, revenue today/week, verification approval rate, a recharts order-status bar chart), `admin.shops.tsx` (list all shops, search/filter, suspend/reactivate), `admin.partners.tsx` (list all partners, search/filter, suspend/reactivate), `admin.orders.tsx` (list all orders, status filter, force-cancel). Backend: `src/lib/admin-data/backend.server.ts` grew `listAllShops`/`suspendShop`/`reactivateShop`, `listAllPartners`/`suspendPartner`/`reactivatePartner`, `listAllOrders`/`cancelOrder`, `getAdminStats`. Scope confirmed via AskUserQuestion: order overrides = force-cancel only (no refund logic — Phase F payments isn't live, nothing to reverse); partner actions = suspend/reactivate only (no new approval-gate-at-signup flow); analytics = simple live aggregates, no time-series storage. Verified end-to-end against the live DB (all new queries + suspend/reactivate/cancel transitions) and via headless render across all 5 pages.
- [x] **Phase H — Realtime (scoped: order status only, polling not websockets — see decisions.md 2026-07-14).** True `postgres_changes` Realtime turned out not to be reachable from the browser: RLS on `orders`/`order_items` only grants the `authenticated` role, and the browser never holds a real Supabase Auth JWT (auth is a custom phone/OTP flow — see `src/lib/auth.tsx`), so an anon-key Realtime subscription would just get zero rows. Implemented "live" status via `refetchInterval` polling (6s) instead: `orders.tsx` (customer order list, now shows a status badge per order — it showed none before), `order.$orderId.tsx` (status badge added next to "Order summary"; polling stops once delivered/cancelled), `src/lib/seller.tsx`'s `shop-orders` query, `src/lib/partner.tsx`'s `my-jobs` query. The fake rider-dot animation (`src/lib/tracking.tsx` / `useDeliverySimulation`) was deliberately left untouched — it's cosmetic and disconnected from real GPS, not in scope this pass, and the user confirmed this explicitly via AskUserQuestion.
- [x] **Real partner GPS tracking (see decisions.md 2026-07-15).** Turned out the location-sharing UI already existed — `src/hooks/useLiveLocation.ts` + the "Share my live location" button on `partner.track.$orderId.tsx` were real `navigator.geolocation.watchPosition` calls, just wired only to the fake local `tracking.tsx` store, never to the database. New `src/lib/tracking-data/{backend.server.ts,api.functions.ts}`: `pushPartnerLocation()` (throttled to 1 write/8s from the partner's live-location callback, inserts into `partner_locations`), `getOrderTracking()` (real order status mapped to the 5-stage `TrackStatus`, plus the assigned partner's latest position, resolved via the `accepted` row in `assignments`). `order.$orderId.tsx` now polls `getOrderTracking` (5s, stops once delivered) and feeds real status/position into the shared `TrackSession` instead of `useDeliverySimulation`'s scripted 72s clock — the fake simulation is no longer used on the customer page at all. Fixed a bug found in passing: `partner.track.$orderId.tsx`'s "Mark ready/delivered" button only wrote to the local fake session (`setStatus`), never called the real `advanceJob` that `JobCard` on `/partner/deliveries` uses — now both call the same real action. The partner's own "Simulate ride (demo)" fallback button was left in place for testing without a device's GPS.
- [x] **Verification pipeline migration (scoped: per-level status only — see decisions.md 2026-07-15).** Investigated first: `src/lib/verification.ts`'s 8-level `ShopVerification` wizard state lives entirely in localStorage, and separately `src/lib/verification/backend.server.ts` already runs a *real* per-file AI-vision analysis pipeline (OCR, duplicate detection, real Storage) for L2 documents and L5 photos — but persists it to the generic `events` table (`mv.document`/`mv.submission`/`mv.audit`), not `kyc_documents`/`shop_photos`. Confirmed nothing in the app read `shop_verifications.l1_phone..l7_review` or any of `kyc_documents`/`bank_verifications`/`gps_verifications`/`shop_photos`/`fraud_flags` — all unused since Phase A. User chose the smallest of three scoped options: sync just the 8 per-level status columns (they already exist, exact enum match with the client's `LevelStatus`) whenever the wizard's `updateVerification` fires, alongside the existing badge/overall-status summary sync — `seller-data/backend.server.ts`'s `syncVerificationSummary` and its validator now take an optional `levels` object. Verified end-to-end against the live DB that all 8 statuses + badge/overall-status round-trip correctly.
- [ ] Real bank/GPS/shop-photo persistence (`bank_verifications`/`gps_verifications`/`shop_photos` tables) — declined this pass, not a security decision like PAN/Aadhaar but still out of scope.
- [ ] PAN/Aadhaar + L2 document records into `kyc_documents` — explicitly declined; needs its own PII-storage decision (no dedicated column for raw PAN/Aadhaar numbers exists yet) before being attempted. The events-table-based `mv.document`/`mv.submission` pipeline in `src/lib/verification/backend.server.ts` keeps working exactly as before; only the wizard's own per-level status roll-up changed.
- [x] `seller.track.$orderId.tsx`'s pickup-pin bug fixed (2026-07-15) — swapped the old mock `getShop()` from `src/lib/data.ts` for the real `@/lib/catalog/api.functions` `getShop`, via `useQuery` keyed on `shop.id`. Since `ensureSession` only sets pickup once and the real shop fetch is async, added a second effect that corrects the pin with `updatePickup` once the query resolves, in case it lands after the session already exists with the `geocodeSeed` fallback.
- [ ] Integrate email provider for verification links.
- [ ] Integrate payment provider for penny-drop bank verification (Razorpay / Cashfree) — part of Phase F/verification migration.
- [x] **Public `/status` page (2026-07-24, see decisions.md).** Real, unauthenticated status page checking web app / database / payments-config state, inspired by openstatus.dev. Auto-refreshes every 30s.
- [ ] Admin-side "possible duplicate shop" hint during verification review — the queue now receives real submissions (see 2026-07-26 below), so this is unblocked and worth picking up.
- [x] Admin verification queue received its first real submission and was approved (2026-07-26, see decisions.md) — no longer untested against real data.
- [x] **`createShop`'s hardcoded lat/lng fixed (2026-07-24, see decisions.md).** Now requires the merchant's real GPS-pinned coordinates (captured client-side via `CreateShopStep.tsx`, same `LocationPinMap`/geolocation pattern customers already use), validated server-side against a sanity bounding box.
- [x] **Real name+proximity shop-match candidates (2026-07-25, see decisions.md).** `find_shop_matches` SQL function (migration 0014, pg_trgm + PostGIS) backs `findPossibleShopMatches`, used by `CreateShopStep.tsx` once a location is pinned. Also returns `claimed` shops (unused by this caller) so an admin-side duplicate hint could reuse it later without a second implementation — not built this pass, still blocked on the admin verification queue never receiving a real submission (see below).
- [x] **Barcode-driven product onboarding (2026-07-25, see decisions.md).** `@zxing/browser` camera scan (with manual-entry fallback) + a real Open Food Facts lookup (`src/lib/barcode/`) prefills new-product fields; `products.barcode` is unique per-shop (migration 0015), and re-scanning an existing barcode jumps to editing that product instead of duplicating it. Coverage is food/grocery-first (Open Food Facts) — a scan for e.g. an electronics/hardware item will usually miss and fall back to manual entry, which is expected, not a bug.
- [ ] `@zxing/library` (a peer of `@zxing/browser`) declares a `node >= 24.0.0` engine requirement; this project runs Node 22.18 — installed with an `EBADENGINE` warning, typechecks/builds/runs clean so far, but worth watching if anything barcode-related misbehaves at runtime, and revisiting on the next Node upgrade.
- [ ] `claimShop`'s update-then-reread sequence intermittently flakes under the dev server (never in a standalone script) — mitigated with a retry wrapper in tests; root cause still unknown, revisit if it resurfaces.
- [ ] Import more cities beyond the current Bengaluru-area OSM coverage; `way`/`relation` OSM geometries (not just point nodes) still unimported; `searchProducts` still has no explicit row limit (same class of bug as the 1000-row `getNearbyShops` cap that was fixed).
- [ ] No password-reset flow exists — deliberately deferred when email OTP was dropped in favor of email+password, since building it would reintroduce an email-deliverability dependency.

## Verification-gate desync fix + canonical product catalog (2026-07-26, see decisions.md)

- [x] **First real shop approved, and a real bug found doing it** — `VerificationLockGate`/`SellerProvider` (`src/lib/seller.tsx`) gated "can I add products" on `verification.overallStatus`, a **localStorage-only** field (matches the known e2e gotcha noted above) that only ever updates from this browser's own wizard actions or the admin UI's same-device convenience flip — neither holds when an admin approves from a different device, which is the normal case. Fixed to gate on `shop.verificationStatus` (the real DB-backed field from `getMyShop`), plus a reconciliation effect that flips local wizard state to match once the real DB says `approved`, so `/seller/verification`'s step list stops showing stale "pending" too.
- [x] **Canonical product catalog, `catalog_products` (migration 0016)** — the "500 shops × the same Maggi = duplicated name/description/image everywhere" problem. Deliberately **not** the full `products` → `shop_products` rename floated in review: `order_items.product_id`/`wishlist.product_id` reference `products(id)` directly and the `place_order_mobile` RPC (0005) joins on it, so re-keying that table would force changes across all three for no immediate payoff. This is the lower-risk version — `products.id` unchanged, a new nullable `catalog_product_id` links barcode-identified rows (barcode-only, no fuzzy name matching) to one shared canonical record. `addProduct`/`updateProduct` now get-or-create the catalog row when a barcode's set; a new `getCatalogProductByBarcode` lookup is checked before the external Open Food Facts call in `seller.products.tsx`, so a barcode any shop already scanned resolves instantly without a repeat external call.
- [ ] **Known gap left out on purpose**: Open Food Facts' `imageUrl`/description aren't captured into `catalog_products` yet — the add-product form doesn't pass an image through at creation time (photos are uploaded separately, after a product exists), so wiring that needs a form/type change beyond this pass.

## Sidebar links + home/shop-page UI fixes (2026-07-25, see decisions.md)

- [x] **Sidebar links to shop/delivery-partner onboarding** — `/seller`/`/partner` were only reachable by URL; added a "Sell & Deliver" nav section.
- [x] **Home-page fixes from a pasted external UX/UI audit** — "Open now" filter toggle, a neutral "New" badge (instead of `★ 0`) for shops with `ratingCount === 0`, and real visual weight on the location-selector pill.
- [~] **Two audit items confirmed real but deferred**: the hero image's garbled AI-generated signage text (no image-gen tool available) and collapsing the persistent sidebar into a drawer (bigger architecture change than a bug fix, sidebar already has its own collapse-to-icons toggle).
- [x] **Shop detail page: Menu/Reviews tabs + a real sidebar direction card** — grounded in Zomato's actual restaurant-page HTML (user pasted the raw source, then screenshots) cross-checked against NearCart's real data before building anything. Sticky tab bar splits the old single-scroll layout; desktop gets a `lg:sticky` sidebar with a read-only map (`LocationPinMap`'s new `interactive={false}` mode), the real address, and Copy/Direction buttons using real `shop.lat`/`lng`.
- [ ] **Not built, real data gap**: a photo gallery/split hero like Zomato's — `shop_photos` table exists in the schema but nothing writes to it anywhere (no upload UI in the seller flow) and `getShop` never reads it. Building the gallery UI now would mean fabricating images; needs a real shop-photo-upload feature first (natural companion to the existing `uploadShopLogo`/`uploadProductImage` pattern in `seller-data/`).
- [ ] **Not applicable**: Zomato's separate Dining/Delivery rating badges and its icon+label "More Info" amenities card (delivery-only/seating/etc.) don't map to NearCart — it's delivery-only with one rating pipeline and no boolean amenity fields on `shops`. Skipped rather than faked.

## Cross-Platform Mobile Apps (React Native + Expo)

- [ ] Set up monorepo structure (Web app and Mobile apps sharing packages/) — `mobile/` exists alongside the web app but no shared `packages/`, no workspaces config
- [ ] Create shared packages for Types, Supabase Client, and GPS/Proximity Utilities
- [x] Initialize Expo Mobile App (targets both iOS and Android) — `mobile/App.tsx`, `app.json`, Expo config all in place
- [x] Implement Mobile Buyer App UI (Discovery, Search, Cart, Orders) — `mobile/src/screens/{Home,ShopDetails,Cart,Orders,Settings}Screen.tsx` (search is inline-filter in `HomeScreen`, not a separate screen)
- [x] **Mobile connected to the real backend (2026-07-18, see decisions.md).** Found and fixed a real regression first: `CartScreen.tsx`/`OrdersScreen.tsx` imported `@/lib/orders`, which no longer exists — deleted during the web app's Phase C months ago, and mobile was never updated (it reaches into the web app's `src/` via a `@/*` → `../src/*` path alias, so it silently broke without anyone touching mobile directly). Architecture decision, confirmed with the user: mobile talks **directly to Supabase** (`@supabase/supabase-js`, already a dependency) with a **real `supabase.auth` session** — not the web app's TanStack Start server functions (unreachable from React Native) and not the web app's custom localStorage auth. New: `mobile/src/lib/{supabase.ts,auth.tsx,catalog.ts,orders.ts}`, `mobile/src/screens/LoginScreen.tsx`, `supabase/migrations/0005_place_order_mobile_rpc.sql` (a `SECURITY DEFINER` RPC — RLS only grants `authenticated` `SELECT` on `orders`/`order_items`, never `INSERT`, so mobile can't place orders with a plain client insert; the RPC mirrors the web app's server-side re-pricing logic in SQL). All 5 screens rewired to real data; `tsc --noEmit` clean; a full Expo web-export bundle smoke test caught a second real bug (`LoginScreen.tsx` used the `@/lib/auth` alias instead of the relative `../lib/auth`, silently resolving to the web app's fake auth module instead of mobile's real one — `tsc` couldn't catch it since both modules happen to export a structurally-identical `useAuth()` shape); fixed and re-verified clean. RPC re-pricing/validation logic + its `auth.uid()`-null rejection guard verified against the live DB.
- [x] **Mobile auth pivoted from phone to email OTP, and a real second bug fixed along the way (2026-07-18, see decisions.md).** Phone sign-in stayed blocked (Twilio needs a paid account to deliver to India — Twilio won't even sell an Indian number to individuals; the fix isn't buying a local number, it's just using any Twilio number as sender, since OTP delivery to India is one-way regardless). Rather than wait on that, pivoted `mobile/src/lib/auth.tsx`/`LoginScreen.tsx`/`SettingsScreen.tsx` to Supabase's built-in **email** OTP instead — no third-party signup needed. While wiring this up, root-caused what the earlier "email createUser 500s" finding actually was: **not** a missing-SMTP issue as first suspected, but a real bug in `handle_new_auth_user()` (`supabase/migrations/0001_initial_schema.sql`) — it inserted `coalesce(new.phone, '')` into a `unique not null` column, so the *first* phone-less signup got `phone = ''` and every one after it collided on that same empty string. Fixed in `0006_users_phone_nullable.sql` (phone now nullable, trigger inserts `new.phone`/`new.email` directly — NULL is correctly excluded from the UNIQUE constraint, unlike `''`). Verified: two consecutive email-only signups no longer collide, and the full `place_order_mobile` RPC path (re-pricing, `auth.uid()` as customer_id, cross-shop rejection, RLS read-back) now verified end-to-end against a **real authenticated session** for the first time this session (previous passes could only reproduce the RPC's logic via service-role, since no real session was obtainable yet).
- [ ] Phone-OTP sign-in — deferred, not abandoned. Needs a Twilio number (any country's — India-bound SMS is one-way regardless of sender, so no local number is required) plus its trial numbers verified as recipients for testing. `mobile/src/lib/auth.tsx` would need a second code path (or a provider toggle) to support both email and phone, since `0006` deliberately made `phone` optional specifically so both can coexist.
- [ ] Implement Mobile Driver App with background GPS location tracking (Expo TaskManager + expo-location)
- [ ] Implement Merchant Verification Wizard screens on mobile (Camera uploads, GPS coordinate captures)
